# 验证清单 (Checklist)

- [x] 后端启动时的终端日志和应用错误提示中，不存在 `AstrBot` 字样。
- [x] 成功使用 PyInstaller 将后端打包为独立的可执行文件（无环境依赖）。
- [x] Tauri 配置成功接入 Sidecar 机制，前端启动时后端服务自动在后台拉起。
- [x] 打包出的 DMG 文件体积显著增加，并在未安装 Python 的干净环境下能够正常聊天。
